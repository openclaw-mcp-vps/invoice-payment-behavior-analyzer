// This repository intentionally does not use Prisma in runtime code.
// The SaaS stores data through direct file persistence to avoid ORM codegen dependencies.
// If you migrate to direct SQL (pg or better-sqlite3), you can remove this file.
